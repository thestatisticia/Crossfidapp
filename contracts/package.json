{
    "name": "crossfi-voting-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for CrossFi voting dApp",
    "scripts": {
        "compile": "hardhat compile",
        "deploy": "hardhat run scripts/deploy.js --network crossfi",
        "test": "hardhat test"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "@openzeppelin/contracts": "^5.3.0",
        "dotenv": "^16.0.3",
        "hardhat": "^2.22.19"
    }
}
